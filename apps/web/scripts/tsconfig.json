{
  "//": "Local config for the tsx-run scripts in this folder. Standalone (no extends) so tsx's tsconfig resolver has nothing to walk; the app's own tsconfig keeps `jsx: preserve` for Next, but tsx needs the automatic runtime to execute the .tsx renderers directly.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "types": ["node"],
    "baseUrl": "..",
    "paths": { "@/*": ["*"] }
  },
  "include": ["**/*", "../lib/**/*"]
}
